using UnityEngine;

// Flash effect is handled by WLayerHUD; this component is kept as a stub
// so FourDSetupWizard.EnsureComponent<WTransitionEffect> doesn't break existing scenes.
public class WTransitionEffect : MonoBehaviour { }
